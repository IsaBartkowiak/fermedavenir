<%= form_for([@farm, @hamper], html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @hamper.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@hamper.errors.count, "error") %> prohibited this hamper from being saved:</h4>

      <ul>
      <% @hamper.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group">
    <%= f.label :week, class: "col-sm-12" %>
    <div class="col-xs-12 col-sm-2 cl-md-2">
      <%= f.number_field :week, class: "form-control", min: 0, max: 52%>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-12">
      <%= f.submit "Créer ce panier", class: "btn btn_green" %>
    </div>
    </div>
<% end %>
<ul class="list-unstyled">
  <% @hamper.portions.each do |portion| %>
  <li><%= portion.plants.name %></li>
  <% end %>
</ul>