<div class="section_grey">
<div class="container">
	<div class="page-header">
	  <%= link_to generations_path, class: 'btn btn-return' do %>
	    <span class="glyphicon glyphicon-menu-left"></span>
	    Retourner à la liste
	  <% end %>
	  <h1>Nouvelle génération</h1>
	</div>

	<%= render 'form' %>
</div>
</div>
