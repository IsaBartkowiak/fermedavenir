<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="">
  <meta name="author" content="">
  <title>Le Tableau de Bord</title>

  <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!--[if lt IE 9]>
    <%= javascript_include_tag "https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js", "https://oss.maxcdn.com/respond/1.4.2/respond.min.js" %>
  <![endif]-->

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <header class="header">
      <a href="/" class="header-logo">
        <%= render('layouts/logo') %>
      </a>
      <% if current_user %>
        <nav class="header-nav">
          <ul>
            <li class="nav-item"><%= link_to "Légumes", legumes_path, class: 'btn' %></li>
            <li class="nav-item"><%= link_to "Paniers", farm_paniers_path(current_user.farm.slug), class: 'btn' unless current_user.farm.nil? %></li>
            <li class="nav-item"><%= link_to "Générations", generations_path, class: 'btn' %></li>
            <li class="nav-item"><%= link_to "Plantations", farm_plantations_path(current_user.farm.slug), class: 'btn' unless current_user.farm.nil? %></li>
          </ul>
        </nav>
      <% end %>
      <div class="header-links">
        <% if !current_user %>
            <%= link_to "Se connecter", new_user_session_path, class: 'btn btn_green btn_stroke' %>
            <%= link_to "S'enregistrer", new_user_registration_path, class: 'btn btn_green' %>
        <% else %>
            <%= link_to "Déconnexion", destroy_user_session_path, method: :delete, class: 'btn btn_green' %>
        <% end %>
      </div>
    </header>
  </div>
    <% flash.each do |name, msg| %>
      <%= content_tag :div, class: "alert alert-#{ name == :error ? "danger" : "success" } alert-dismissable", role: "alert" do %>
        <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
        <%= msg %>
      <% end %>
    <% end %>

    <%= yield %>
  <footer class="footer">
    <div class="container">
      <div class="row tablehome">
        <div class="col-xs-12 col-sm-4 cellfooter">
        <p>© Copyright 2016</p></div>
        <div class="col-xs-12 col-sm-4 cellfooter">
          <p>En partenariat avec :</p>
          <%= link_to image_tag('logo-fermesdavenir-white.png', height: 90), "http://www.fermesdavenir.org", class: "footer-logo" %>
        </div>
        <div class="col-xs-12 col-sm-4 cellfooter">
        <p>Retrouvez nous sur Facebook</p>
      <%= link_to image_tag('facebook.png', class:"img_petit"), "https://www.facebook.com/letableaudebord" %></div>
      </div>
    </div>
  </footer>
</body>
</html>
